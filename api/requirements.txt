# API and Engine dependencies for Vercel deployment
# All dependencies are listed explicitly to avoid path resolution issues

# FastAPI and server
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Image processing and 3D reconstruction
numpy>=1.26
opencv-python-headless>=4.9
Pillow>=10.2
trimesh>=4.3
scikit-image>=0.22
fast-simplification>=0.1.7

# Install engine as editable package using absolute path for Vercel
# Vercel deploys the entire project to /vercel/path0
-e /vercel/path0/engine
